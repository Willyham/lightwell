[package]
name = "lightwell-evidence"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish.workspace = true

[lib]
name = "lightwell_evidence"
path = "src/lib.rs"

# Serde types only: the desktop parses a script with them and xtask writes one, so the crate
# depends on neither the editor nor the core.
[dependencies]
serde.workspace = true
serde_json.workspace = true
zeroize.workspace = true

[lints]
workspace = true
