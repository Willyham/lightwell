{
  "schema_version": "1.0",
  "plan_id": "consolidation",
  "title": "Consolidation",
  "objective": "Give each cross-cutting mechanism one implementation that every feature extends, fix the defects where copies diverged, and delete the copies, with no product change beyond the owner's 2026-09-24 decisions.",
  "context_summary": "The whole-codebase review of 2026-09-24 found most cross-cutting mechanisms implemented two or more times by parallel feature work, with confirmed defects where copies disagree. The design lists each concept, its copies and its replacement, and the owner's decisions are recorded. Wave 1 fixes defects and deletes; later waves consolidate the core, the module contract, the desktop, rendering and the harness.",
  "tasks": [
    {
      "id": "TASK-001",
      "description": "Make a deduplicated retry emit no event, refuse an unrouted method explicitly, give Restore the same admission checks as every commit, and plan each preset step against its own target so presets work on masked photos.",
      "status": "completed",
      "dependencies": [],
      "extra_context": [
        "A deduplicated mutation result keeps `outcome: applied`, and the owner's event check reads only the outcome, so a retried request records a second event.",
        "A method with no service handler that no owner layer matches falls through to the `events.since` answer; it must fail as an internal error naming the method.",
        "`EditorService::restore` runs only registry recipe validation, while `commit_snapshot` also validates the source recipe, records and requires artifacts and compiles. One admission function serves both, and no copy of the checks stays behind.",
        "A preset's `Compose` steps plan against the whole resolved recipe, while a direct action plans against `recipe_for_target`. With a global and a masked Basic layer a preset is refused as ambiguous; with only a masked one it silently edits the masked layer. `capture_preset` reads layers the same way."
      ],
      "context_links": [
        {
          "kind": "file",
          "label": "Consolidation design",
          "target": "docs/design/consolidation.md",
          "relevance": "Constraints and the one-path-per-concept table this task follows."
        },
        {
          "kind": "file",
          "label": "Editor service",
          "target": "crates/lightwell-core/src/editor.rs",
          "relevance": "`restore`, `commit_snapshot`, `resolve_plan`, `recipe_for_target`."
        },
        {
          "kind": "file",
          "label": "API methods and owner",
          "target": "crates/lightwell-core/src/api/methods.rs",
          "relevance": "`mutates` and dispatch; the owner loop's fallthrough is in `api/owner.rs`."
        },
        {
          "kind": "file",
          "label": "Preset library",
          "target": "crates/lightwell-core/src/presets/library.rs",
          "relevance": "`capture_preset` reads a module's one layer."
        },
        {
          "kind": "file",
          "label": "Presets design",
          "target": "docs/design/presets.md",
          "relevance": "Composite actions and capture."
        }
      ],
      "acceptance_criteria": [
        "A retried mutation that is answered from the request table records no event; the first attempt still records one.",
        "An unrouted handler-less method is refused with an internal error naming it.",
        "Restore and every other commit admit a recipe through one function, and Restore refuses what a commit refuses (for example a stack naming a missing stroke).",
        "Applying and capturing a preset on a stack with a global and a masked Basic layer uses the global layer, and on a stack with only a masked Basic layer creates or reads a global one; the masked layer is untouched."
      ],
      "test_strategy": {
        "approach": "unit",
        "steps": [
          "Add a test per acceptance criterion beside the code it covers.",
          "Run the existing history, deduplication, restore, preset and mask command tests."
        ],
        "commands": [
          "cargo test -p lightwell-core",
          "cargo xtask verify --tier quick"
        ],
        "expected_results": [
          "The new tests fail before the change and pass after it; existing tests pass unchanged."
        ]
      }
    },
    {
      "id": "TASK-002",
      "description": "Send a mask gesture's `draft.set` synchronously on the desktop thread, as the slider gesture does, and stop a late answer from presenting a drafted frame after Discard.",
      "status": "completed",
      "dependencies": [],
      "extra_context": [
        "The slider calls `tasks::draft_set_now` synchronously because a runtime hop costs one display frame during a drag (performance rule 12). The mask gesture sends the same request through `Task::perform` in `mask_draft_set_task`, and the measured paint latency misses its bound.",
        "`mask_cancel` sends `draft.cancel` while a `draft.set` may be in flight, and the `MaskDraftSet` handler stores the draft and requests its preview without checking that the gesture still exists. The slider defers its cancel until the in-flight answer arrives.",
        "The single desktop draft driver comes in wave 4; this task changes only the hop and the race, reusing `draft_set_now`.",
        "A comment in `app/masks.rs` still calls the overlay for a pixel-reading mask (P16) open; it is built."
      ],
      "context_links": [
        {
          "kind": "file",
          "label": "Consolidation design",
          "target": "docs/design/consolidation.md",
          "relevance": "Constraints and the one-path-per-concept table this task follows."
        },
        {
          "kind": "file",
          "label": "Desktop owner tasks",
          "target": "crates/lightwell-app/src/app/tasks.rs",
          "relevance": "`draft_set_now` and `mask_draft_set_task`."
        },
        {
          "kind": "file",
          "label": "Mask gesture driver",
          "target": "crates/lightwell-app/src/app/masks.rs",
          "relevance": "The mask draft lifecycle, cancel and set handlers."
        },
        {
          "kind": "file",
          "label": "Performance rules",
          "target": "docs/engineering/performance-rules.md",
          "relevance": "Rule 12."
        }
      ],
      "acceptance_criteria": [
        "A mask gesture's `draft.set` runs synchronously through the same helper the slider uses, and `mask_draft_set_task` is gone.",
        "An answer that arrives after Discard presents no frame and leaves no draft.",
        "Mask gesture, brush and gradient tests pass."
      ],
      "test_strategy": {
        "approach": "unit",
        "steps": [
          "Add a desktop test in which a late set answer arrives after cancel and nothing is displayed.",
          "Run the mask desktop tests."
        ],
        "commands": [
          "cargo test -p lightwell-app masks",
          "cargo xtask verify --tier quick"
        ],
        "expected_results": [
          "The late answer is dropped; existing mask tests pass."
        ]
      }
    },
    {
      "id": "TASK-003",
      "description": "Key a spatial unit's global estimate by what its preparation reads, reduce the stage only when a missing unit needs an estimate, describe units without rounding, decide proxy approximation from the compiled operations, and take test-only counters and estimate hooks out of the public API.",
      "status": "completed",
      "dependencies": [],
      "extra_context": [
        "`resolve_globals` keys each unit by its `describe()` string, which includes the amount, so every new Presence value misses and reduces the whole stage on both preview phases. Clarity and Texture prepare no estimate yet still trigger the reduction; Dehaze's estimate does not depend on its amount.",
        "Unit `describe()` strings round their values although they are used as identities (operation equality and the estimate key).",
        "`proxy_approximate` decides by effect stage, so a reset Presence layer, which compiles to nothing, labels exact proxy frames approximate.",
        "`masked_tile_counts`, `reset_masked_tile_counts`, `cached_estimates` and `clear_estimates` are exported from `lib.rs` and used only by tests; the masked-tile counters are process-global atomics bumped on the hot path."
      ],
      "context_links": [
        {
          "kind": "file",
          "label": "Consolidation design",
          "target": "docs/design/consolidation.md",
          "relevance": "Constraints and the one-path-per-concept table this task follows."
        },
        {
          "kind": "file",
          "label": "Spatial rendering",
          "target": "crates/lightwell-core/src/render/spatial.rs",
          "relevance": "`resolve_globals`, the estimate store and the masked-tile counters."
        },
        {
          "kind": "file",
          "label": "Presence units",
          "target": "crates/lightwell-core/src/modules/presence/mod.rs",
          "relevance": "`prepare` and `describe` of dehaze, clarity and texture."
        },
        {
          "kind": "file",
          "label": "Registry",
          "target": "crates/lightwell-core/src/modules/registry.rs",
          "relevance": "`proxy_approximate`."
        },
        {
          "kind": "file",
          "label": "Modules and API",
          "target": "docs/design/modules-and-api.md",
          "relevance": "The global-estimate contract to update."
        }
      ],
      "acceptance_criteria": [
        "Changing only a Presence amount prepares from the stored estimate without a new reduction, and an operation whose units need no estimate never reduces.",
        "Rendered bytes and samples are unchanged on both paths.",
        "A stack whose only spatial layer is neutral is not labelled approximate.",
        "The test hooks are no longer public API; tests still observe the counts.",
        "The modules-and-api design describes the new estimate key."
      ],
      "test_strategy": {
        "approach": "unit",
        "steps": [
          "Count reductions in tests for an amount change and for an estimate-free operation.",
          "Run the spatial, presence, proxy and masked-spatial tests."
        ],
        "commands": [
          "cargo test -p lightwell-core",
          "cargo xtask verify --tier quick"
        ],
        "expected_results": [
          "Reductions drop as stated; exactness tests pass unchanged."
        ]
      }
    },
    {
      "id": "TASK-004",
      "description": "Keep at most two spatial frames alive on the RAW path, stop copying every finished frame into a new allocation, allocate colour scratch once per worker, delete render code only tests use, and correct the RAW Planckian coefficient.",
      "status": "completed",
      "dependencies": [],
      "extra_context": [
        "`LinearEvaluation` keeps every spatial segment's full float frame for the whole evaluation, up to five at 60 MP, against the architecture's two-frame rule.",
        "Renders end by converting `Vec<u8>` into `Arc<[u8]>`, which allocates and copies the whole frame; the linear and proxy paths do the same. The colour pass allocates a fresh scratch buffer per row chunk.",
        "`render::locate`, `Evaluation::locate`, `locate_in` and `ProxyCache::is_empty` are used only by tests; production uses `locate_dimensions`.",
        "The RAW white-balance locus uses 0.2343580e6 where the published Kim et al. coefficient, the Basic copy, its reference and the design use 0.2343589e6. The shared colour module arrives in wave 3."
      ],
      "context_links": [
        {
          "kind": "file",
          "label": "Consolidation design",
          "target": "docs/design/consolidation.md",
          "relevance": "Constraints and the one-path-per-concept table this task follows."
        },
        {
          "kind": "file",
          "label": "RAW linear path",
          "target": "crates/lightwell-core/src/render/linear.rs",
          "relevance": "Spatial frame retention and the linear output."
        },
        {
          "kind": "file",
          "label": "Byte render path",
          "target": "crates/lightwell-core/src/render.rs",
          "relevance": "Frame output, colour scratch and `locate`."
        },
        {
          "kind": "file",
          "label": "Proxy",
          "target": "crates/lightwell-core/src/proxy.rs",
          "relevance": "Proxy frame output."
        },
        {
          "kind": "file",
          "label": "RAW white balance",
          "target": "crates/lightwell-core/src/modules/raw/white_balance.rs",
          "relevance": "The locus coefficient."
        }
      ],
      "acceptance_criteria": [
        "At most two spatial frames are alive at once on the RAW path, proved by a test.",
        "No render, linear or proxy output is copied after it is written; buffer sharing is proved by a test.",
        "Rendered bytes and samples are unchanged.",
        "The test-only locate code is deleted and its tests use `locate_dimensions`.",
        "The coefficient is corrected and the RAW white-balance tests pass."
      ],
      "test_strategy": {
        "approach": "unit",
        "steps": [
          "Add the frame-retention and buffer-sharing tests.",
          "Run the render, linear, proxy, spatial and RAW tests."
        ],
        "commands": [
          "cargo test -p lightwell-core",
          "cargo xtask verify --tier quick"
        ],
        "expected_results": [
          "New tests pass; exactness tests pass unchanged."
        ]
      }
    },
    {
      "id": "TASK-005",
      "description": "Remove `artifact.relocate`, the grant's last-use record and the `read-user-file` capability with the `file` setting kind, across the core, the desktop, the proof module, the capabilities smoke and the docs.",
      "status": "completed",
      "dependencies": [],
      "extra_context": [
        "The owner decided these on 2026-09-24. `artifact.relocate` has no caller: the artifact directory moves with its catalog. Recording a grant's last use rewrites and syncs the grants file on the owner after every task and install, for a display field.",
        "`read-user-file` and the `file` setting kind are used only by the proof module's `input` capability; `preset.import` takes the file's text from its client. The module capabilities design keeps a one-line note that a module reading a user-chosen file may add them back, consented per canonical path."
      ],
      "context_links": [
        {
          "kind": "file",
          "label": "Consolidation design",
          "target": "docs/design/consolidation.md",
          "relevance": "Constraints and the one-path-per-concept table this task follows."
        },
        {
          "kind": "file",
          "label": "Capabilities",
          "target": "crates/lightwell-core/src/capabilities/host.rs",
          "relevance": "Grants, consent, files, context, host and settings."
        },
        {
          "kind": "file",
          "label": "Artifact store",
          "target": "crates/lightwell-core/src/editor/artifact_store.rs",
          "relevance": "Relocation and the artifact root override."
        },
        {
          "kind": "file",
          "label": "Desktop capabilities",
          "target": "crates/lightwell-app/src/state/capabilities.rs",
          "relevance": "File settings and grant display."
        },
        {
          "kind": "file",
          "label": "Module capabilities design",
          "target": "docs/design/module-capabilities.md",
          "relevance": "The contract to update."
        },
        {
          "kind": "file",
          "label": "Decisions",
          "target": "docs/decisions.md",
          "relevance": "Module capabilities, revised 2026-09-24."
        }
      ],
      "acceptance_criteria": [
        "No code, test or doc refers to `artifact.relocate`, a grant's last use, `read-user-file` or the `file` setting kind, apart from the one note in the design.",
        "Remote-request and download consent, activation, tasks and artifacts behave as before.",
        "The capabilities smoke scenario's script and checks no longer use the removed pieces."
      ],
      "test_strategy": {
        "approach": "unit",
        "steps": [
          "Delete the removed code and its tests; keep every test of the remaining behaviour.",
          "Run the capability, artifact and desktop capability tests."
        ],
        "commands": [
          "cargo test -p lightwell-core capabilities",
          "cargo test -p lightwell-app capabilities",
          "cargo xtask verify --tier quick"
        ],
        "expected_results": [
          "The remaining capability behaviour passes unchanged."
        ]
      }
    },
    {
      "id": "TASK-006",
      "description": "Replace the hand-written HTTP/1.1 client in the module transport with a pinned, well-known and tested HTTP client behind the existing address policy.",
      "status": "completed",
      "dependencies": [],
      "extra_context": [
        "The owner decided on 2026-09-24 to use a well-known client such as `ureq` instead of the project's own HTTP parser.",
        "The decided policy stays: HTTPS with platform trust for remote endpoints, public addresses only, plain HTTP only to loopback, no proxies, no redirect followed, the connection made to the address the policy checked with no second resolution, bounded headers and bodies, and a cancel noticed within the current read slice.",
        "Dependencies are pinned exactly and must pass the repository's license and advisory policy. The executable size and package count change is recorded where the current figure is."
      ],
      "context_links": [
        {
          "kind": "file",
          "label": "Consolidation design",
          "target": "docs/design/consolidation.md",
          "relevance": "Constraints and the one-path-per-concept table this task follows."
        },
        {
          "kind": "file",
          "label": "Transport",
          "target": "crates/lightwell-core/src/capabilities/transport/mod.rs",
          "relevance": "`http.rs` goes; `policy.rs` stays."
        },
        {
          "kind": "file",
          "label": "Module capabilities design",
          "target": "docs/design/module-capabilities.md",
          "relevance": "Transport section."
        },
        {
          "kind": "file",
          "label": "Architecture",
          "target": "docs/design/architecture.md",
          "relevance": "Workspace dependency list."
        }
      ],
      "acceptance_criteria": [
        "The HTTP parser is deleted and requests go through the pinned client.",
        "Every policy behaviour the transport tests cover still holds: address classes, loopback-only HTTP, refused redirects, size limits, chunked bodies, timeouts and cancellation.",
        "The capabilities proof journey passes.",
        "The dependency policy check passes and the size change is recorded."
      ],
      "test_strategy": {
        "approach": "unit",
        "steps": [
          "Port the transport tests to the new client without weakening any assertion.",
          "Run the transport and capability tests."
        ],
        "commands": [
          "cargo test -p lightwell-core transport",
          "cargo test -p lightwell-core capabilities",
          "cargo xtask verify --tier quick"
        ],
        "expected_results": [
          "Every ported test passes against the new client."
        ]
      }
    },
    {
      "id": "TASK-007",
      "description": "Delete `cargo xtask raw-reference` and the `probe` command with `probes/s0`, remove the duplicate 33 ms latency target, and run the `raw-panel` and RAW `performance` scenarios in the `full` tier when a manifest is given.",
      "status": "completed",
      "dependencies": [],
      "extra_context": [
        "`raw-reference` asserts values against literals in its own file and calls no production code; the production RAW tests cover what it claims.",
        "The probe served the decided S0 toolkit selection; the decision stays recorded in the research docs.",
        "One verify target still reads the superseded 33 ms figure from the same JSON path as the 16/32 ms slider target.",
        "The `raw-panel` scenario and a RAW `performance` run are not in any tier."
      ],
      "context_links": [
        {
          "kind": "file",
          "label": "Consolidation design",
          "target": "docs/design/consolidation.md",
          "relevance": "Constraints and the one-path-per-concept table this task follows."
        },
        {
          "kind": "file",
          "label": "xtask",
          "target": "xtask/src/main.rs",
          "relevance": "`main.rs`, `verify.rs`, `raw.rs`, `diagnostics.rs`, `smoke.rs`."
        },
        {
          "kind": "file",
          "label": "Development",
          "target": "docs/engineering/development.md",
          "relevance": "Tier contents and command lists."
        },
        {
          "kind": "file",
          "label": "Architecture",
          "target": "docs/design/architecture.md",
          "relevance": "Mentions `probes/s0`."
        }
      ],
      "acceptance_criteria": [
        "The two commands, their code and their docs are gone; `raw-corpus` stays.",
        "The summary has one slider latency target.",
        "`full` with `--manifest` includes `raw-panel` for each manifest source and one RAW `performance` run."
      ],
      "test_strategy": {
        "approach": "inspection",
        "steps": [
          "Build xtask and list the tier plan for `full` with and without a manifest."
        ],
        "commands": [
          "cargo test -p xtask",
          "cargo xtask verify --tier quick"
        ],
        "expected_results": [
          "The plan lists the new components; nothing references the removed commands."
        ]
      }
    },
    {
      "id": "TASK-008",
      "description": "Cut the run-log prose from the masking design, the Masks row of feature status and the range study's P16, delete the completed masking and UI-components plans, replace the AGENTS.md current-state paragraph with a short pointer, and single-source the slider budget and the RAW admission limits.",
      "status": "completed",
      "dependencies": [],
      "extra_context": [
        "AGENTS.md forbids change logs, run logs and planning history in docs. The masking design's status line is one paragraph of about 2,600 words, the Masks feature row about 2,100 and the range study's P16 row about 1,000, mostly evidence narrative.",
        "The masking and UI-components plans have every task completed; completed plans are deleted and their outcome lives in the specs.",
        "AGENTS.md's current-state paragraph is read every session, says catalog format 6 (the code is at 7), and never mentions masking; its map row also names catalog format 2.",
        "The slider budget is restated in six docs and the RAW admission limits in ten. The performance spec owns the budget and the architecture's rendering and limits section owns the limits; others link.",
        "Stale statements to fix while there: the masking design still calls P16 not yet built, and the performance spec says no `mask.*` command posts a stroke yet."
      ],
      "context_links": [
        {
          "kind": "file",
          "label": "Consolidation design",
          "target": "docs/design/consolidation.md",
          "relevance": "Constraints and the one-path-per-concept table this task follows."
        },
        {
          "kind": "file",
          "label": "AGENTS.md",
          "target": "AGENTS.md",
          "relevance": "Current state and map."
        },
        {
          "kind": "file",
          "label": "Masking design",
          "target": "docs/design/masking.md",
          "relevance": "Status line."
        },
        {
          "kind": "file",
          "label": "Feature status",
          "target": "docs/features.md",
          "relevance": "Masks row."
        },
        {
          "kind": "file",
          "label": "Range study",
          "target": "docs/design/range-study.md",
          "relevance": "Proposal P16."
        },
        {
          "kind": "file",
          "label": "Task plans",
          "target": "tasks/README.md",
          "relevance": "Plan table and deletion convention."
        }
      ],
      "acceptance_criteria": [
        "Each cut status keeps only current behaviour, its verification in a sentence and outstanding work, at the length of its sibling docs.",
        "The two completed plans and their README rows are gone and no link points at them.",
        "AGENTS.md's current state is a short pointer to feature status that names masking and states no version numbers.",
        "Each budget and limit is stated in one doc and linked from the others.",
        "Decisions, the consolidation design and this plan are not edited."
      ],
      "test_strategy": {
        "approach": "inspection",
        "steps": [
          "Check links and plan schemas."
        ],
        "commands": [
          "cargo xtask check-repository"
        ],
        "expected_results": [
          "All links and plans validate."
        ]
      }
    },
    {
      "id": "TASK-009",
      "description": "Split `editor.rs` into an `editor/` module along its seams — service and types, catalog storage, history transactions, source preparation, evaluation, planning, mask glue and describe — as a pure move with no behaviour change, so the rest of wave 2 edits separate files.",
      "status": "completed",
      "dependencies": [
        "TASK-001",
        "TASK-003",
        "TASK-004",
        "TASK-005",
        "TASK-006"
      ],
      "extra_context": [
        "`editor.rs` is about 7.7k lines, half of it tests, and one `impl EditorService` holds catalog SQL, transactions, source preparation, evaluation, planning, mask glue and describe. Every wave 2 task edits it, so it is split first by moving code, not by changing it.",
        "Function bodies stay as they are; only module placement, visibility and imports change. Tests move with the code they test. The preset library reaches into the service's connection directly; give it narrow catalog functions instead if that is a move, not a redesign."
      ],
      "context_links": [
        {
          "kind": "file",
          "label": "Consolidation design",
          "target": "docs/design/consolidation.md",
          "relevance": "Constraints and the one-path-per-concept table."
        },
        {
          "kind": "file",
          "label": "Editor service",
          "target": "crates/lightwell-core/src/editor.rs",
          "relevance": "The file to split."
        },
        {
          "kind": "file",
          "label": "Architecture",
          "target": "docs/design/architecture.md",
          "relevance": "Crate layout to update."
        }
      ],
      "acceptance_criteria": [
        "`editor.rs` becomes `editor/` with one file per seam and a small `mod.rs`; `git diff -M` shows the change as moves plus visibility and imports.",
        "Every test passes unchanged in name and assertion.",
        "The architecture doc names the new layout."
      ],
      "test_strategy": {
        "approach": "unit",
        "steps": [
          "Build and run the whole core suite once at the end."
        ],
        "commands": [
          "cargo test -p lightwell-core",
          "cargo xtask verify --tier quick"
        ],
        "expected_results": [
          "All tests pass unchanged."
        ]
      }
    },
    {
      "id": "TASK-010",
      "description": "Wave 3, module contract: the declarative field-patch module, a plan result whose layer identity, format and mask the host owns, declared single-layer effects, `builtin_modules()` with core-reported neutrality, declared source kinds with one Exposure and white-balance control set for JPEG and RAW, one colour module, descriptor builders with one schema emitter, mask commands through the shared plan path, and capability settings on the parameter vocabulary.",
      "status": "pending",
      "dependencies": [
        "TASK-009"
      ],
      "extra_context": [
        "Detailed when wave 2 lands. The source-kind control set needs its own short design first: how one control behaves per source kind."
      ],
      "context_links": [
        {
          "kind": "file",
          "label": "Consolidation design",
          "target": "docs/design/consolidation.md",
          "relevance": "Constraints and the one-path-per-concept table this task follows."
        }
      ],
      "acceptance_criteria": [
        "Descriptors other than the decided control consolidation are unchanged, proved by comparing them before and after.",
        "Renders are byte-identical."
      ],
      "test_strategy": {
        "approach": "mixed",
        "steps": [
          "Targeted tests per task; rendered once on the integrated wave."
        ],
        "commands": [
          "cargo xtask verify --tier quick",
          "cargo xtask verify --tier rendered"
        ],
        "expected_results": [
          "Every tier passes."
        ]
      }
    },
    {
      "id": "TASK-011",
      "description": "Wave 4, desktop: one core-draft driver and gesture field, `app/mod.rs` split with nested messages, a presenter for the displayed frame, overlays on the photo surface, a silent idle poll, per-message waste removed, and crop on the core draft lifecycle.",
      "status": "pending",
      "dependencies": [
        "TASK-002",
        "TASK-009"
      ],
      "extra_context": [
        "Detailed when wave 2 lands."
      ],
      "context_links": [
        {
          "kind": "file",
          "label": "Consolidation design",
          "target": "docs/design/consolidation.md",
          "relevance": "Constraints and the one-path-per-concept table this task follows."
        }
      ],
      "acceptance_criteria": [
        "Gesture latency and idle CPU are no worse, measured once at the end.",
        "UI/API parity holds."
      ],
      "test_strategy": {
        "approach": "mixed",
        "steps": [
          "Targeted tests per task; rendered and timing once on the integrated wave."
        ],
        "commands": [
          "cargo xtask verify --tier quick",
          "cargo xtask verify --tier rendered",
          "cargo xtask verify --tier timing"
        ],
        "expected_results": [
          "Every tier passes."
        ]
      }
    },
    {
      "id": "TASK-012",
      "description": "Wave 5, rendering and RAW: one latest-job worker primitive for preview, analysis and the clipping overlay, one compile per job, one render entry point and a render context in place of globals, band-parallel RAW development, row evaluation on the RAW path, then one pipeline for both domains.",
      "status": "pending",
      "dependencies": [
        "TASK-009"
      ],
      "extra_context": [
        "Detailed when wave 2 lands."
      ],
      "context_links": [
        {
          "kind": "file",
          "label": "Consolidation design",
          "target": "docs/design/consolidation.md",
          "relevance": "Constraints and the one-path-per-concept table this task follows."
        }
      ],
      "acceptance_criteria": [
        "Renders and samples are byte-identical except where a task proves a decided bound.",
        "Memory stays within the architecture's limits."
      ],
      "test_strategy": {
        "approach": "mixed",
        "steps": [
          "Targeted tests per task; rendered, timing and full once on the integrated wave."
        ],
        "commands": [
          "cargo xtask verify --tier quick",
          "cargo xtask verify --tier full"
        ],
        "expected_results": [
          "Every tier passes; timing improves where claimed."
        ]
      }
    },
    {
      "id": "TASK-013",
      "description": "Wave 6, harness: a smoke scenario library and scenario table, one conformance suite for field-patch modules, a serde-derived evidence script shared with xtask, test-support and independent-reference crates, and one timing distribution type.",
      "status": "pending",
      "dependencies": [
        "TASK-007"
      ],
      "extra_context": [
        "Detailed after wave 1. Old and new verifiers are compared over the same captured artifacts before the old ones are deleted."
      ],
      "context_links": [
        {
          "kind": "file",
          "label": "Consolidation design",
          "target": "docs/design/consolidation.md",
          "relevance": "Constraints and the one-path-per-concept table this task follows."
        }
      ],
      "acceptance_criteria": [
        "Every existing check still runs, and a deliberately broken frame or step fails the new harness where it failed the old one."
      ],
      "test_strategy": {
        "approach": "mixed",
        "steps": [
          "Replay existing rendered artifacts through old and new verifiers."
        ],
        "commands": [
          "cargo xtask verify --tier quick",
          "cargo xtask verify --tier rendered"
        ],
        "expected_results": [
          "Identical check results on the same artifacts."
        ]
      }
    },
    {
      "id": "TASK-014",
      "description": "Bound a point query through more than one spatial segment: each spatial tile is evaluated at most once per query, through one tile cache shared by the byte and RAW point paths, so a sample equals the rendered byte at a bounded cost.",
      "status": "completed",
      "dependencies": [
        "TASK-004"
      ],
      "extra_context": [
        "On the byte path a point sample through two spatial segments runs a whole inner tile for every pixel of the outer tile and for every pixel the global reduction reads, with no caching; measured in release, a centre sample on a 48×36 image goes from 0.1 ms with one spatial layer to 187 ms with two, and at the real 512 px tile it is effectively a hang. A global plus a masked Presence layer on a JPEG reaches it through `render.sample`, grid samples and the layer-input overlay, all on the catalog owner.",
        "The RAW point path keeps up to four evaluated tiles for the rest of a request so a resample's four neighbours evaluate each tile once; the byte path has no such cache, so a sample through Presence and a straightened crop also runs the tile chain four or five times.",
        "The fix is one cache for both paths, bounded by the spatial budget, not a second mechanism."
      ],
      "context_links": [
        {
          "kind": "file",
          "label": "Consolidation design",
          "target": "docs/design/consolidation.md",
          "relevance": "Constraints."
        },
        {
          "kind": "file",
          "label": "Byte render path",
          "target": "crates/lightwell-core/src/render.rs",
          "relevance": "`Evaluation`, `spatial_pixel` and the point path."
        },
        {
          "kind": "file",
          "label": "RAW linear path",
          "target": "crates/lightwell-core/src/render/linear.rs",
          "relevance": "`PointSegment` and its tile cache."
        },
        {
          "kind": "file",
          "label": "Modules and API",
          "target": "docs/design/modules-and-api.md",
          "relevance": "The point-query cost contract through a spatial segment."
        }
      ],
      "acceptance_criteria": [
        "Each spatial segment's tile is evaluated at most once per point query on both paths, proved by counting tile evaluations in tests with two and three spatial segments and under a straightened crop.",
        "A sample equals the rendered byte on both paths for those stacks.",
        "One cache serves both paths.",
        "The design and performance rules state the bounded cost of a point query through several spatial segments."
      ],
      "test_strategy": {
        "approach": "unit",
        "steps": [
          "Count tile evaluations per query for multi-segment stacks on both paths.",
          "Run the render, spatial, linear and masked-spatial tests."
        ],
        "commands": [
          "cargo test -p lightwell-core",
          "cargo xtask verify --tier quick"
        ],
        "expected_results": [
          "Counts are bounded as stated; exactness tests pass unchanged."
        ]
      }
    },
    {
      "id": "TASK-015",
      "description": "Route every host method through one table whose entries are service or owner handlers, declare each method's parameters once so the schema and the parser cannot differ, and give every mutating method the same `{request_id, actor}` envelope with deduplication.",
      "status": "completed",
      "dependencies": [
        "TASK-001"
      ],
      "extra_context": [
        "Today 31 handler-less methods are routed by three string-match layers in the owner loop plus the capability host's own match, and the event append is copied three times beside `record_event`.",
        "Parameters are declared as required and optional strings in `METHODS`, again as about 37 serde structs, and again in a hand-copied schema test; the parser exists four times and clones the request; five methods ignore their parameters, against the rule that a misspelt filter is never silently ignored.",
        "The owner decided on 2026-09-24 that every mutating method carries `{request_id, actor}` and is deduplicated, with `expected_revision` wherever a revision exists; today presets, versions, resources and artifacts use four different conventions."
      ],
      "context_links": [
        {
          "kind": "file",
          "label": "Consolidation design",
          "target": "docs/design/consolidation.md",
          "relevance": "Constraints and the one-path-per-concept table."
        },
        {
          "kind": "file",
          "label": "API methods",
          "target": "crates/lightwell-core/src/api/methods.rs",
          "relevance": "The METHODS table and handlers."
        },
        {
          "kind": "file",
          "label": "Owner loop",
          "target": "crates/lightwell-core/src/api/owner.rs",
          "relevance": "Owner-answered methods and events."
        },
        {
          "kind": "file",
          "label": "Capability host",
          "target": "crates/lightwell-core/src/capabilities/host.rs",
          "relevance": "`answer`'s own method match."
        },
        {
          "kind": "file",
          "label": "Modules and API",
          "target": "docs/design/modules-and-api.md",
          "relevance": "The API contract."
        },
        {
          "kind": "file",
          "label": "Decisions",
          "target": "docs/decisions.md",
          "relevance": "Architecture review: the mutation envelope."
        }
      ],
      "acceptance_criteria": [
        "One table dispatches every host method; the owner loop has no per-method string match and no fallthrough.",
        "Each method's parameters are declared once and generate both the schema and the parser; every method refuses an unknown parameter.",
        "Every mutating method takes `{request_id, actor}` and a retry returns the first answer; the desktop, `lightwell-json`, xtask and the docs use the new shapes.",
        "The event recording, held-draft lookup and selected-entry resolution each exist once."
      ],
      "test_strategy": {
        "approach": "unit",
        "steps": [
          "A generated test sends every listed method its declared fields and gets no unknown-field error, and one unknown field gets a validation error.",
          "A retry test per mutating method family."
        ],
        "commands": [
          "cargo test -p lightwell-core",
          "cargo test -p lightwell-app",
          "cargo xtask verify --tier quick"
        ],
        "expected_results": [
          "All pass; schema.list matches the parsers by construction."
        ]
      }
    },
    {
      "id": "TASK-016",
      "description": "Remove the S0 viewer loader from `lib.rs` and move JPEG decoding into `source.rs` with one size limit, decoding straight into the shared frame instead of copying it.",
      "status": "completed",
      "dependencies": [
        "TASK-004"
      ],
      "extra_context": [
        "`Loader`, `Photo`, `DecodeTimings`, `open()` and `read_bounded` serve only their own tests and one xtask fixture check that needs width, height and orientation, which `open_source` also returns.",
        "The 128 MiB JPEG limit is defined twice.",
        "`open_source_bytes` copies the decoded pixels into a new `Arc<[u8]>`, 96 MB at 24 MP; `render::zeroed_frame` gives a frame to write into once."
      ],
      "context_links": [
        {
          "kind": "file",
          "label": "Consolidation design",
          "target": "docs/design/consolidation.md",
          "relevance": "Constraints and the one-path-per-concept table."
        },
        {
          "kind": "file",
          "label": "Core lib",
          "target": "crates/lightwell-core/src/lib.rs",
          "relevance": "The loader and decode code."
        },
        {
          "kind": "file",
          "label": "Source",
          "target": "crates/lightwell-core/src/source.rs",
          "relevance": "Where decoding belongs."
        },
        {
          "kind": "file",
          "label": "xtask fixtures",
          "target": "xtask/src/fixtures.rs",
          "relevance": "The one external user of `open`."
        }
      ],
      "acceptance_criteria": [
        "The loader, `Photo`, `DecodeTimings`, `open()` and `read_bounded` are gone and `lib.rs` is re-exports and module declarations.",
        "JPEG decoding lives in `source.rs` with one limit constant and writes RGBA into its shared frame once.",
        "Decoded bytes are identical and the input-contract tests pass against `open_source`."
      ],
      "test_strategy": {
        "approach": "unit",
        "steps": [
          "Port the input-contract tests to `open_source`; run the source and fixture checks."
        ],
        "commands": [
          "cargo test -p lightwell-core",
          "cargo xtask fixtures check",
          "cargo xtask verify --tier quick"
        ],
        "expected_results": [
          "Identical decode results."
        ]
      }
    },
    {
      "id": "TASK-017",
      "description": "Cache the catalog owner's immutable history entries with their strokes resolved, share a recipe's stroke table instead of deep-copying it, give drafts a revision read that decodes nothing, and store the RAW interpretation typed instead of re-parsing JSON on every read.",
      "status": "completed",
      "dependencies": [
        "TASK-009"
      ],
      "extra_context": [
        "Every `state()` and `entry()` decodes the entry's JSON and hydrates its strokes (a SQL lookup, a SHA-256 and a parse per referenced stroke); one slider tick reads the state three times, one only to compare the revision, and `render.sample` three times.",
        "Entries are immutable, enforced by the catalog's trigger, and the owner is the only writer over an exclusive connection, so a small cache of entries keyed by entry id and a head per asset updated by every write is coherent by construction.",
        "`StrokeTable` derives a deep `Clone` over a map of point vectors, so every recipe clone copies every stroke; it is immutable after hydration.",
        "`SourceKind::Raw { metadata: Value }` is re-parsed on every state read and in every source-recipe validation, and compared through a JSON round trip to work around f32/f64 inequality."
      ],
      "context_links": [
        {
          "kind": "file",
          "label": "Consolidation design",
          "target": "docs/design/consolidation.md",
          "relevance": "Constraints and the one-path-per-concept table."
        },
        {
          "kind": "file",
          "label": "Editor catalog",
          "target": "crates/lightwell-core/src/editor/catalog.rs",
          "relevance": "Entry decode and stroke hydration."
        },
        {
          "kind": "file",
          "label": "Editor reads",
          "target": "crates/lightwell-core/src/editor/describe.rs",
          "relevance": "`state` and `entry`."
        },
        {
          "kind": "file",
          "label": "Paths and strokes",
          "target": "crates/lightwell-core/src/path.rs",
          "relevance": "`StrokeTable`."
        },
        {
          "kind": "file",
          "label": "Editor source",
          "target": "crates/lightwell-core/src/editor/source.rs",
          "relevance": "RAW metadata and source-recipe validation."
        }
      ],
      "acceptance_criteria": [
        "A state or entry read after the first costs no JSON decode or stroke hash, proved by a test that counts them; every write keeps the cache coherent, proved by tests over commit, undo, redo, restore, import and reopen.",
        "Cloning a recipe does not copy its strokes.",
        "A draft's conflict check reads the revision without decoding an entry.",
        "The RAW interpretation is typed and parsed once per asset row."
      ],
      "test_strategy": {
        "approach": "unit",
        "steps": [
          "Counting tests for decodes and hashes; coherence tests per write path; the existing history, mask and RAW tests."
        ],
        "commands": [
          "cargo test -p lightwell-core",
          "cargo xtask verify --tier quick"
        ],
        "expected_results": [
          "All pass; reads after the first decode nothing."
        ]
      }
    },
    {
      "id": "TASK-018",
      "description": "Give the smoke scenarios one shared library for the launch envelope, frame accessors and pixel readers, with every scenario's checks unchanged, proved by replaying the old and new verifiers over the same captured artifacts.",
      "status": "completed",
      "dependencies": [
        "TASK-007"
      ],
      "extra_context": [
        "Nine runners hand-write the same launch envelope (output directory, hashes, spawn, wait, preamble, exit codes, source-unchanged check, result and reproduce files); `revision`, `label`, `entry`, `draft` and section accessors are copied across up to twelve files; the photograph's bounds are found six ways with three thresholds; patch readers are copied and re-decode the same capture several times per frame.",
        "The result's fixed `launch1_exit_code`…`launch3_exit_code` keys are counted by verify and would miss a fourth launch; a launch list replaces them.",
        "The wave 1 rendered run's artifacts are the replay input: the old and new verifiers must produce the same checks over the same output directories."
      ],
      "context_links": [
        {
          "kind": "file",
          "label": "Consolidation design",
          "target": "docs/design/consolidation.md",
          "relevance": "Constraints and the one-path-per-concept table."
        },
        {
          "kind": "file",
          "label": "Smoke runner",
          "target": "xtask/src/smoke.rs",
          "relevance": "Scenario list, dispatch and shared preamble."
        },
        {
          "kind": "file",
          "label": "Development",
          "target": "docs/engineering/development.md",
          "relevance": "Smoke and verify docs."
        }
      ],
      "acceptance_criteria": [
        "One module holds the launch envelope, the frame accessors and the pixel readers, and no scenario file re-implements them.",
        "Replaying every scenario's verifier over the same captured output directories gives identical check results before and after, and a deliberately corrupted state value or PNG fails both.",
        "Results record their launches as a list and verify reads it."
      ],
      "test_strategy": {
        "approach": "mixed",
        "steps": [
          "A `--verify-only DIR` replay over the wave 1 rendered artifacts with the old and new binaries, diffing every checks file."
        ],
        "commands": [
          "cargo test -p xtask",
          "cargo xtask verify --tier quick",
          "cargo xtask verify --tier rendered"
        ],
        "expected_results": [
          "Identical checks; the rendered tier passes."
        ]
      }
    },
    {
      "id": "TASK-019",
      "description": "Make Basic, the colour mixer, Presence and the vignette one declarative field-patch module each (a field table plus `compile`), let the host own a planned layer's identity, format and mask, declare single-layer effects with one host lookup of a module's own layer, build the desktop's and the registry's module lists from one function with an availability override, and have the core report whether a stored layer is neutral.",
      "status": "completed",
      "dependencies": [
        "TASK-001",
        "TASK-015"
      ],
      "extra_context": [
        "The four field-patch modules are about 85% the same code: descriptor building, parse, plan with commit or update, payload validation, values, labels, describe and neutrality, plus per-module copies of `validation`, `number`, `canonical`, `is_neutral`, `read_payload`, `locate` and group-reset helpers. Their `plan` re-checks ranges the host's generic check already ran.",
        "`ActionPlan::Update(Layer)` replaces the whole layer, so every module must remember to copy the host-owned mask; `model.rs` imports a module payload type and holds module effect constants and constructors.",
        "\"The module's one layer\" is looked up about eight times, two of which ignore mask targets (the preset defect TASK-001 fixed at the call site); `single_layer()` is a trait method where a declared effect flag would do.",
        "The desktop re-lists `ModuleRegistry::builtin()` and wraps disabled modules in its own `Disabled` provider, with three more copies in tests; it decides neutrality by parsing core payloads and gets field-patch modules wrong, which is known bug TASK-001 (a section's dot stays lit after Reset)."
      ],
      "context_links": [
        {
          "kind": "file",
          "label": "Consolidation design",
          "target": "docs/design/consolidation.md",
          "relevance": "Constraints and the one-path-per-concept table."
        },
        {
          "kind": "file",
          "label": "Basic module",
          "target": "crates/lightwell-core/src/modules/basic/mod.rs",
          "relevance": "A field-patch module."
        },
        {
          "kind": "file",
          "label": "Registry",
          "target": "crates/lightwell-core/src/modules/registry.rs",
          "relevance": "`builtin`, single-layer checks and placement."
        },
        {
          "kind": "file",
          "label": "Module trait",
          "target": "crates/lightwell-core/src/modules/mod.rs",
          "relevance": "`ToolModule`, `ActionPlan`, `StageContext`."
        },
        {
          "kind": "file",
          "label": "Desktop module list",
          "target": "crates/lightwell-app/src/app/mod.rs",
          "relevance": "The re-listed modules and `Disabled`."
        },
        {
          "kind": "file",
          "label": "Desktop tools state",
          "target": "crates/lightwell-app/src/state/tools.rs",
          "relevance": "`neutral`."
        },
        {
          "kind": "file",
          "label": "Modules and API",
          "target": "docs/design/modules-and-api.md",
          "relevance": "The module contract."
        }
      ],
      "acceptance_criteria": [
        "Each field-patch module is its field table, its `compile` and (Basic) its query; the shared behaviour exists once; `schema.list` and `module.list` output is identical before and after, proved by comparing them.",
        "A plan names the effect and payload and the host assigns identity, format and mask, preserving the mask on update; module effect constants and constructors leave `model.rs`.",
        "A single-layer effect is declared on its effect descriptor and every lookup of a module's own layer goes through one host function that respects the mask target.",
        "One function lists the built-in modules for the registry and the desktop, with an availability override; no `Disabled` wrapper remains.",
        "`recipe.describe` reports each layer's neutrality from its module and the desktop's section dot reads it, closing the known bug.",
        "Rendered bytes are identical."
      ],
      "test_strategy": {
        "approach": "unit",
        "steps": [
          "Descriptor equality before and after; the existing module, preset, mask and describe tests; a dot test for every module with a neutral form."
        ],
        "commands": [
          "cargo test -p lightwell-core",
          "cargo test -p lightwell-app",
          "cargo xtask verify --tier quick"
        ],
        "expected_results": [
          "All pass with identical descriptors and renders."
        ]
      }
    },
    {
      "id": "TASK-020",
      "description": "Give source, analysis and capability jobs one status vocabulary and one status key, and have capability jobs report progress through the activity board instead of their own progress model.",
      "status": "completed",
      "dependencies": [
        "TASK-015"
      ],
      "extra_context": [
        "Source jobs answer `state: queued|preparing|ready|failed|cancelled` with string ids; analysis jobs `status: pending|ready|failed|superseded|cancelled`; capability jobs `status: queued|running|succeeded|failed|cancelled|superseded`; even source-job answers mix `state` and `status`.",
        "Capability jobs keep their own progress fraction and message and are not published to the activity board that `activity.list` reads, which the roadmap already names as follow-up; the desktop polls `module.job.read` every 100 ms."
      ],
      "context_links": [
        {
          "kind": "file",
          "label": "Consolidation design",
          "target": "docs/design/consolidation.md",
          "relevance": "Constraints and the one-path-per-concept table."
        },
        {
          "kind": "file",
          "label": "Owner",
          "target": "crates/lightwell-core/src/api/owner.rs",
          "relevance": "Source jobs."
        },
        {
          "kind": "file",
          "label": "Analysis jobs",
          "target": "crates/lightwell-core/src/analysis/jobs.rs",
          "relevance": "Analysis status."
        },
        {
          "kind": "file",
          "label": "Capability jobs",
          "target": "crates/lightwell-core/src/capabilities/jobs.rs",
          "relevance": "Capability status and progress."
        },
        {
          "kind": "file",
          "label": "Activity board",
          "target": "crates/lightwell-core/src/activity.rs",
          "relevance": "Where long-running work is published."
        },
        {
          "kind": "file",
          "label": "Performance panel design",
          "target": "docs/design/performance-panel.md",
          "relevance": "The activity board contract."
        }
      ],
      "acceptance_criteria": [
        "One job-status type and one `status` key serve all three job kinds and their API answers, with the kind-specific states documented.",
        "Capability jobs appear on the activity board with their progress, and their own progress model is gone.",
        "The desktop and docs use the new shapes."
      ],
      "test_strategy": {
        "approach": "unit",
        "steps": [
          "Update the job and activity tests; add one showing a capability job's progress on the board."
        ],
        "commands": [
          "cargo test -p lightwell-core",
          "cargo test -p lightwell-app",
          "cargo xtask verify --tier quick"
        ],
        "expected_results": [
          "All pass."
        ]
      }
    },
    {
      "id": "TASK-021",
      "description": "Drive every desktop gesture that uses the core draft lifecycle through one pure draft state machine and one gesture field, so the one-draft-per-client rule is a type invariant, and fix each draft race the mask work found, each with a test.",
      "status": "completed",
      "dependencies": [
        "TASK-002",
        "TASK-015"
      ],
      "extra_context": [
        "The slider keeps its draft in `SliderDraft`; the mask gesture keeps the same state machine as loose `Editor` fields; begin, commit and reapply tasks and their messages exist twice; the conflict rule exists three times; the one-draft rule is hand-checked at about a dozen sites with different subsets.",
        "Races found in the mask gesture: Apply before `draft.begin` answers loses the commit; a late `draft.begin` answer is adopted by a newer gesture; Discard during an in-flight commit races it; a reapply answer carries no draft id; the slider's Discard does not cancel queued drafted frames; `draft.cancel` and a `session.state` read race; the proxy refit ignores the mask draft.",
        "The crop's desktop-local draft moves onto the core lifecycle in a later task; keep its gesture separate here but make it share the one gesture field and refusal."
      ],
      "context_links": [
        {
          "kind": "file",
          "label": "Consolidation design",
          "target": "docs/design/consolidation.md",
          "relevance": "Constraints and the one-path-per-concept table."
        },
        {
          "kind": "file",
          "label": "Slider driver",
          "target": "crates/lightwell-app/src/app/slider.rs",
          "relevance": "`SliderDraft` and its lifecycle."
        },
        {
          "kind": "file",
          "label": "Mask driver",
          "target": "crates/lightwell-app/src/app/masks.rs",
          "relevance": "The mask gesture's draft fields and handlers."
        },
        {
          "kind": "file",
          "label": "Desktop tasks",
          "target": "crates/lightwell-app/src/app/tasks.rs",
          "relevance": "Draft owner calls."
        },
        {
          "kind": "file",
          "label": "Performance rules",
          "target": "docs/engineering/performance-rules.md",
          "relevance": "Rule 12: the synchronous `draft.set`."
        }
      ],
      "acceptance_criteria": [
        "One pure draft state machine and one `gesture` field serve the slider and mask gestures; the duplicated tasks, messages and conflict rules are gone.",
        "One function answers whether a new gesture may start, and every site uses it.",
        "Each listed race has a test that fails before the change and passes after.",
        "`draft.set` stays synchronous for both gestures."
      ],
      "test_strategy": {
        "approach": "unit",
        "steps": [
          "Unit tests of the state machine; desktop tests for each race; the existing slider and mask tests."
        ],
        "commands": [
          "cargo test -p lightwell-app",
          "cargo xtask verify --tier quick"
        ],
        "expected_results": [
          "All pass."
        ]
      }
    },
    {
      "id": "TASK-022",
      "description": "Give every catalog write one path: a `mutate` that validates the envelope, answers a retry, checks the revision, runs the caller's plan, admits the recipe once and writes one transaction that also updates the entry cache; one `write` helper and `From<rusqlite::Error>` for every transaction, including the preset library and the artifact store.",
      "status": "completed",
      "dependencies": [
        "TASK-015",
        "TASK-017"
      ],
      "extra_context": [
        "Write transactions are hand-built for import, commit, navigate, restore, version create and delete and persisted no-ops; `HistoryEntry` and `MutationResult` literals and the redo-clearing SQL are copied; one commit validates its recipe several times, including again inside `insert_entry`'s transaction.",
        "Restore's transaction has no request-absent check, so a concurrent duplicate is a catalog error rather than a conflict; `commit_snapshot` assigns `state.current_entry` to a value it then drops; a doc comment meant for `insert_entry` sits on `default_artifact_root`.",
        "About 95 `.map_err(catalog_error)` and 13 `transaction_with_behavior` blocks repeat across the editor, the preset library and the artifact store.",
        "The entry cache updates after each committed write; with one write path it updates in one place."
      ],
      "context_links": [
        {
          "kind": "file",
          "label": "Consolidation design",
          "target": "docs/design/consolidation.md",
          "relevance": "Constraints and the one-path-per-concept table."
        },
        {
          "kind": "file",
          "label": "Editor history",
          "target": "crates/lightwell-core/src/editor/history.rs",
          "relevance": "Commit, navigate, restore, versions, no-ops."
        },
        {
          "kind": "file",
          "label": "Editor catalog",
          "target": "crates/lightwell-core/src/editor/catalog.rs",
          "relevance": "`insert_entry` and request rows."
        },
        {
          "kind": "file",
          "label": "Entry cache",
          "target": "crates/lightwell-core/src/editor/entries.rs",
          "relevance": "Updated by every write."
        },
        {
          "kind": "file",
          "label": "Preset library",
          "target": "crates/lightwell-core/src/presets/library.rs",
          "relevance": "Its own transactions."
        },
        {
          "kind": "file",
          "label": "History spec",
          "target": "docs/specs/edit-history.md",
          "relevance": "The history contract."
        }
      ],
      "acceptance_criteria": [
        "Commit, undo, redo, restore, versions and no-ops go through one `mutate`; import and the preset library and artifact store use the one `write` helper; the copied literals and SQL exist once.",
        "A recipe is validated once per commit.",
        "A concurrent duplicate of any mutation is a `conflict`, never a catalog error.",
        "The entry cache is updated in one place and its coherence test still passes.",
        "History, deduplication, rollback, artifact and preset tests pass unchanged."
      ],
      "test_strategy": {
        "approach": "unit",
        "steps": [
          "Existing history, dedup, rollback, restore, artifact, preset and cache-coherence tests; a validation-count test; a duplicate-request test for restore."
        ],
        "commands": [
          "cargo test -p lightwell-core",
          "cargo xtask verify --tier quick"
        ],
        "expected_results": [
          "All pass; one validation per commit."
        ]
      }
    },
    {
      "id": "TASK-023",
      "description": "Register each smoke scenario once in a table and pair every scripted step with its expectations, so the runner derives the script and the frame count, applies the universal per-frame checks to every scenario, and leaves each scenario only its own pixel predicates.",
      "status": "in_progress",
      "dependencies": [
        "TASK-018"
      ],
      "extra_context": [
        "Adding a scenario edits six places in `smoke.rs` (the list, dispatch, the verify chain, the source match, the window-size chains) plus the docs; frame counts are hand-kept constants apart from the script; about 1,250 `frames[N]` indices tie checks to positions, so inserting a step renumbers every later check.",
        "The universal invariants are opted into per file: input-error checks in 16 of 20 files, the recorded-step check in 10.",
        "`zoom_smoke::PLAN` already derives its script and frame count from one table. Per-scenario tolerance helpers are still copied with their own thresholds.",
        "`smoke --verify-only` replays a verifier over a recorded run; the wave 1 rendered runs are the replay input."
      ],
      "context_links": [
        {
          "kind": "file",
          "label": "Consolidation design",
          "target": "docs/design/consolidation.md",
          "relevance": "Constraints and the one-path-per-concept table."
        },
        {
          "kind": "file",
          "label": "Smoke runner",
          "target": "xtask/src/smoke.rs",
          "relevance": "Scenario registration and dispatch."
        },
        {
          "kind": "file",
          "label": "Scenario library",
          "target": "xtask/src/scenario/launch.rs",
          "relevance": "The shared launch envelope and replay."
        },
        {
          "kind": "file",
          "label": "Development",
          "target": "docs/engineering/development.md",
          "relevance": "Scenario docs."
        }
      ],
      "acceptance_criteria": [
        "A scenario is registered by one table row; `smoke --list` and verify read the table.",
        "Scripted steps carry their expectations and the runner derives each launch's script and frame count; checks address steps by name or position in the plan, not by hand-kept frame indices.",
        "Provenance, state-equals-frame, recorded-step and input-error checks run on every frame of every scenario.",
        "Replaying the recorded wave 1 runs gives the same verdicts; removing one step or changing one expected label fails the migrated scenario on that step."
      ],
      "test_strategy": {
        "approach": "mixed",
        "steps": [
          "Replay every recorded wave 1 run with old and new verifiers; mutation checks per migrated scenario."
        ],
        "commands": [
          "cargo test -p xtask",
          "cargo xtask verify --tier quick",
          "cargo xtask verify --tier rendered"
        ],
        "expected_results": [
          "Same verdicts on replay; the rendered tier passes."
        ]
      }
    },
    {
      "id": "TASK-024",
      "description": "Carry a recipe's prepared artifacts on the recipe, as its strokes are, so compiling reads them from the recipe instead of a process-global weak index kept alive by pins that every caller must remember.",
      "status": "completed",
      "dependencies": [
        "TASK-022"
      ],
      "extra_context": [
        "Compilation resolves a layer's artifacts through a process-global `Mutex<HashMap<ArtifactId, Weak<PreparedArtifact>>>`, so correctness depends on every caller holding strong references: a dozen `let _artifacts = self.require_artifacts(..)` pins in the editor plus artifact fields on preview jobs, analysis jobs and sample plans. Forgetting one is a runtime `artifact … is not prepared` error.",
        "Strokes already solve the same problem: `Recipe.strokes` is `#[serde(skip)]`, filled when an entry is read, excluded from equality, and compilation refuses its recorded faults.",
        "A binding step that finds an artifact not prepared still answers `preparation-required` exactly as today."
      ],
      "context_links": [
        {
          "kind": "file",
          "label": "Consolidation design",
          "target": "docs/design/consolidation.md",
          "relevance": "Constraints and the one-path-per-concept table."
        },
        {
          "kind": "file",
          "label": "Artifacts",
          "target": "crates/lightwell-core/src/artifacts/mod.rs",
          "relevance": "The global index."
        },
        {
          "kind": "file",
          "label": "Model",
          "target": "crates/lightwell-core/src/model.rs",
          "relevance": "`Recipe` and its stroke table."
        },
        {
          "kind": "file",
          "label": "Registry",
          "target": "crates/lightwell-core/src/modules/registry.rs",
          "relevance": "Where compile resolves artifacts."
        },
        {
          "kind": "file",
          "label": "Module capabilities design",
          "target": "docs/design/module-capabilities.md",
          "relevance": "Derived artifacts."
        }
      ],
      "acceptance_criteria": [
        "A recipe carries its bound artifacts beside its strokes; compile reads them from the recipe.",
        "The global index, the pins and the jobs' artifact fields are gone.",
        "An unprepared artifact still answers `preparation-required`; the artifact, proof and capability tests pass unchanged, including the pixel-exact proof render."
      ],
      "test_strategy": {
        "approach": "unit",
        "steps": [
          "Existing artifact, proof and preview tests; a test that a recipe missing its bound artifact refuses to compile by name."
        ],
        "commands": [
          "cargo test -p lightwell-core",
          "cargo xtask verify --tier quick"
        ],
        "expected_results": [
          "All pass."
        ]
      }
    },
    {
      "id": "TASK-025",
      "description": "Give every timing tool one distribution type with one percentile definition and one JSON shape, and one process sampler, so `verify` reads every timing report the same way.",
      "status": "completed",
      "dependencies": [
        "TASK-007"
      ],
      "extra_context": [
        "Four percentile implementations write four shapes: `editor_latency` (`count/p50_ms/p95_ms/min/max`), `editor_performance` (`samples_ms/p50_ms/p95_ms`), `raw_editor` (`n/first/p50/p95/max`) and `diagnostics` (an interpolated median and a different p95 index); `verify`'s collector special-cases each.",
        "The process-usage sampler is byte-identical in two files, there are four child-watch loops, RSS timestamps are in seconds in one tool and milliseconds in another, and the settle-then-idle window is written twice.",
        "Past figures may shift slightly where the percentile definition changes; say so where figures are recorded."
      ],
      "context_links": [
        {
          "kind": "file",
          "label": "Consolidation design",
          "target": "docs/design/consolidation.md",
          "relevance": "Constraints and the one-path-per-concept table."
        },
        {
          "kind": "file",
          "label": "Editor latency",
          "target": "xtask/src/editor_latency.rs",
          "relevance": "One of the timing tools."
        },
        {
          "kind": "file",
          "label": "Verify",
          "target": "xtask/src/verify.rs",
          "relevance": "The timing collector."
        },
        {
          "kind": "file",
          "label": "Performance spec",
          "target": "docs/specs/performance.md",
          "relevance": "Where figures are recorded."
        }
      ],
      "acceptance_criteria": [
        "One `Distribution` type with a nearest-rank percentile serves every timing tool and writes one shape; `verify` collects with one loop.",
        "One process sampler and one child-watch loop serve every tool.",
        "Golden unit tests pin the percentile on known vectors."
      ],
      "test_strategy": {
        "approach": "unit",
        "steps": [
          "Golden percentile tests; verify's collector tests."
        ],
        "commands": [
          "cargo test -p xtask",
          "cargo xtask verify --tier quick"
        ],
        "expected_results": [
          "All pass."
        ]
      }
    },
    {
      "id": "TASK-026",
      "description": "Answer `history.list` with rows read from columns instead of full recipe snapshots, and scope the desktop's refresh after a command to what the command changed, so a commit costs the owner calls performance rule 7 allows.",
      "status": "in_progress",
      "dependencies": [
        "TASK-021",
        "TASK-022",
        "TASK-024"
      ],
      "extra_context": [
        "After a commit the desktop's `refresh` makes about thirteen owner calls: `asset.state`, `history.list` of 50 entries, `version.list`, `history.lineage`, `session.state`, `recipe.describe`, `mask.list`, a one-row `history.list` only to find the Original's id, and `preview_still_current` twice (two calls each) around the preview job. Rule 7 allows the command, `asset.state` and one preview job, plus what the panels need.",
        "Every `history.list` row carries its entry's full snapshot (masks included, up to 256 KiB each); the desktop's state and view never read a row's snapshot. Each page decodes every row.",
        "Rows need id, sequence, action, label, actor, timestamp, undo parent and restore target; the entries table has columns for some and the rest are inside the entry JSON. Adding columns bumps the catalog format, which current shapes only allows.",
        "The preview queue already tags jobs with a generation, which is what `preview_still_current` re-checks by round trip."
      ],
      "context_links": [
        {
          "kind": "file",
          "label": "Consolidation design",
          "target": "docs/design/consolidation.md",
          "relevance": "Constraints and the one-path-per-concept table."
        },
        {
          "kind": "file",
          "label": "Editor history",
          "target": "crates/lightwell-core/src/editor/history.rs",
          "relevance": "`history`, the page."
        },
        {
          "kind": "file",
          "label": "Editor catalog",
          "target": "crates/lightwell-core/src/editor/catalog.rs",
          "relevance": "Schema and entry rows."
        },
        {
          "kind": "file",
          "label": "Desktop refresh",
          "target": "crates/lightwell-app/src/app/tasks.rs",
          "relevance": "`refresh`, `ready_preview_job`, `preview_still_current`."
        },
        {
          "kind": "file",
          "label": "Performance rules",
          "target": "docs/engineering/performance-rules.md",
          "relevance": "Rules 7 and 10."
        },
        {
          "kind": "file",
          "label": "Versions and lineage",
          "target": "docs/design/versions-and-lineage.md",
          "relevance": "History rows and lineage."
        }
      ],
      "acceptance_criteria": [
        "`history.list` answers rows without snapshots, read without decoding entry JSON; `history.inspect` still returns the full entry.",
        "After an ordinary commit the desktop makes only the calls the changed panels need, counted by a test; undo, redo and restore add lineage; the Original's id is read once per asset open.",
        "The preview job is not wrapped in round-trip currency checks.",
        "History, lineage, branch and versions tests pass; the catalog format bump refuses older catalogs explicitly."
      ],
      "test_strategy": {
        "approach": "unit",
        "steps": [
          "An owner-call count test for commit, undo and restore refreshes; history paging and lineage tests."
        ],
        "commands": [
          "cargo test -p lightwell-core",
          "cargo test -p lightwell-app",
          "cargo xtask verify --tier quick"
        ],
        "expected_results": [
          "All pass with the counted calls."
        ]
      }
    }
  ],
  "execution_waves": [
    {
      "wave": 1,
      "task_ids": [
        "TASK-001",
        "TASK-002",
        "TASK-003",
        "TASK-004",
        "TASK-005",
        "TASK-006",
        "TASK-007",
        "TASK-008"
      ]
    },
    {
      "wave": 2,
      "task_ids": [
        "TASK-009",
        "TASK-013",
        "TASK-014",
        "TASK-015",
        "TASK-016",
        "TASK-018",
        "TASK-025"
      ]
    },
    {
      "wave": 3,
      "task_ids": [
        "TASK-010",
        "TASK-011",
        "TASK-012",
        "TASK-017",
        "TASK-019",
        "TASK-020",
        "TASK-021",
        "TASK-023"
      ]
    },
    {
      "wave": 4,
      "task_ids": [
        "TASK-022"
      ]
    },
    {
      "wave": 5,
      "task_ids": [
        "TASK-024"
      ]
    },
    {
      "wave": 6,
      "task_ids": [
        "TASK-026"
      ]
    }
  ]
}
