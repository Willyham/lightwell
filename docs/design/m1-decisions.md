# M1 accepted decisions

Status: **accepted by the owner on 2026-09-19**, who answered “Adopt these recommendations and choose routine details.” This closes product TASK-026–030 after recording the personal workflow priorities. It is not implementation. Existing module/API decisions remain authoritative.

| Task | Accepted initial behavior | Decision disposition |
| --- | --- | --- |
| TASK-026 workspace | One workspace, photo centered, collapsible editing panel; no library grid in M1. Cmd/Ctrl+O imports, Cmd/Ctrl+Z undoes, Shift+Cmd/Ctrl+Z redoes. Fit/100% and editable percentages are visible. Tab order follows visual control order | Accepted; frustrations recorded in decisions.md |
| TASK-027 geometry | Flip mirrors the visible composition and carries the crop. Quarter-turn carries the crop and swaps locked ratio orientation. Presets Free, Original, 1:1, 3:2, 4:3, 16:9 plus a custom ratio; no numeric crop edges initially. Space+drag pans; crop handles remain distinct. Fine straightening limited to ±45°, quarter-turn controls handle larger rotation | Accepted; fitting mathematics remains TASK-004 |
| TASK-028 history/recovery | Apply commits one crop draft, Cancel discards it; committed state autosaves transactionally with visible success/failure. The core action log and undo/redo survive restart; unapplied drafts do not. The subsequent owner clarification requires browsing, preview and restore of every retained history point (see below). Manual Locate verifies content before updating paths, retains stable identity/edits, and never silently accepts changed or ambiguous originals | Accepted |
| TASK-029 export | JPEG quality 90, native destination picker with a suggested `-edited.jpg` name, reject existing files and all source aliases. Keep metadata off by default; when on, retain supported descriptive/capture/GPS fields and rebuild correct orientation/dimensions/profile, remove stale thumbnails. No arbitrary private-tag promise, resizing or batch export | Accepted; exact field feasibility is TASK-004 |
| TASK-030 live agents | Human and agent commits share attributed history. Agent commits preserve an active human draft and mark it conflicted. The user can discard it or explicitly reapply against the latest revision; stale revisions never overwrite silently. One catalog owner; reconnect queries state; disconnect does not cancel another client's jobs | Accepted |

Agreed centered proportional Option scaling, composition-preserving straightening, default metadata stripping, live MCP, by-reference import and manual Locate are preserved. Geometry math, metadata feasibility and executable golden cases follow in TASK-004 after the existing S0 and M1 gates. Before/after examples: an off-center subject and its crop move together on horizontal flip; a landscape 3:2 crop becomes portrait 2:3 on a clockwise quarter-turn; a changed source with the same name is rejected by Locate; an agent commit preserves an unapplied human draft as conflicted until discard or explicit reapply against the latest revision. Future exposure, masks, clone tools and external-module runtime choices are not added to this slice.

Owner priority input is now recorded in [decision notes](../decisions.md#owner-workflow-priorities--2026-09-19): speed, unused-tool bloat and clear export are immediate drivers; filtering/tagging/collections, lazy shoot views, multi-selection/stacking and bracket/panorama identification guide later scoped library work. The owner separately accepted the recommendations and delegated routine details.

## History clarification — 2026-09-19

The owner explicitly requires the historical log of editing operations as a core feature used by every tool, with preview and restore at any point. This extends the earlier undo/redo wording; it does not reopen the accepted Apply/Cancel, persistence or draft decisions. The [history specification](../specs/edit-history.md) distinguishes durable actions, read-only preview, restore, shortcut navigation and session state. Product TASK-028/030 retain their completed decisions; TASK-070 records the follow-up decision. On 2026-09-20 the owner accepted appending an explicit Restore action while retaining all subsequent entries. Undoing Restore returns to its preceding current state; redo reapplies it. New edits can clear shortcut redo availability without deleting historical entries.
